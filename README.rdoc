== Learning System API

Developed with rails 4.2.0, ruby 2.1.2
Using rails-api gem and initialized with scaffolds

=== To Install:

create databases in mySQL: ls_development, ls_test, ls_cucumber
clone this repository
run rake db:migrate

After confirm migratons, run the server

e.g rails s -p 3001
